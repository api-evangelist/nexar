generated: '2026-08-26'
method: probed
status: published
source: https://getnexar.com/api/mcp
deployment:
  mode: remote
  endpoint: https://getnexar.com/api/mcp
  auth: none
  verified: probed
server:
  name: nexar-storefront
  transport: http
  url: https://getnexar.com/api/mcp
  platform: Shopify Storefront MCP
note: 'HONEST SCOPE: this is the Shopify Storefront MCP endpoint served on Nexar''s own consumer dashcam store at
  getnexar.com. It answered an anonymous JSON-RPC tools/list with HTTP 200 and returned one real tool. It is platform-provided
  by Shopify rather than engineered by Nexar, and it exposes the STORE (policies, FAQs, products), NOT the CityStream
  data APIs. There is no MCP server for VirtualCam, Live Feed, Work Zones or Road Inventory: mcp.getnexar.com does
  not resolve, and POSTing tools/list to external.getnexar.com/mcp and developer.getnexar.com/mcp returns 403 and
  an SPA shell respectively. The corresponding RFC 9728 protected-resource document is saved at well-known/nexar-shopify-oauth-protected-resource.json
  and advertises the Shopify scope customer-account-mcp-api:full for the authenticated customer-account MCP surface.'
probe:
  method: tools/list
  http_status: 200
  tool_count: 1
  raw: mcp/nexar-mcp-tools-list.json
  fetched: '2026-08-26'
tools:
- name: search_shop_policies_and_faqs
  description: Used to get facts about the stores policies, products, or services.
  input_schema:
    query: string (required) — a natural language query
    context: string — additional information about the request
  source: live tools/list
