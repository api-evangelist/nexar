generated: '2026-08-26'
method: searched
source: https://github.com/getnexar/homebrew-tap (README.md + Formula/nap.rb)
name: nap
version: 3.3.17
license: MIT
homepage: https://www.corp.nexars.ai
description: CLI for deploying apps to the Nexar AI Platform.
install:
- method: homebrew
  command: brew install getnexar/tap/nap
  tap: brew tap getnexar/tap
  note: Always use the full path — homebrew-core ships a different `nap`.
- method: direct-download
  platforms:
  - darwin-amd64
  - darwin-arm64
  - linux-amd64
  - linux-arm64
  url_pattern: https://storage.googleapis.com/nexar-cli-releases/v3.3.17/nap-{os}-{arch}
  checksums: sha256 pinned per platform in Formula/nap.rb
commands:
- command: nap version
  description: Print the installed CLI version.
  source: https://github.com/getnexar/homebrew-tap README.md
commands_note: Only `nap version` is documented in the public tap README. The full command surface is not published
  on any public page — the CLI targets the Nexar AI Platform at corp.nexars.ai, which is not a public developer
  surface. Nothing further is invented here.
history:
- version: 3.0.0
  change: Binary renamed from `nexar` to `nap`.
  source: homebrew-tap README.md
relationship_to_apis: The `nap` CLI is a platform-deployment tool. It is NOT a client for the CityStream APIs; there
  is no first-party CLI for VirtualCam, Live Feed, Work Zones or Road Inventory.
